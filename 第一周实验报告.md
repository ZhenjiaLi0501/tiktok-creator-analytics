# 第一周实验报告

本周实验目标主要包括以下几个方面：

1. 完成项目业务需求拆解与技术方案设计，明确项目定位、技术栈、业务模块、路由结构和数据模型。
2. 初始化 Next.js + TypeScript 项目脚手架，保证项目可以正常启动和运行。
3. 接入 TailwindCSS v3，为后续页面 UI 和响应式布局提供样式基础。
4. 配置 ESLint、Prettier 和 Tailwind class 排序规则，统一代码质量和代码格式。
5. 配置 Husky、lint-staged 和 commitlint，实现 Git 提交门禁。
6. 接入 MSW Mock 服务，完成基础接口 Mock 能力。
7. 设计整体路由架构和页面模块划分，为后续业务开发提供清晰目录结构。

## 实验环境

操作系统：Windows
包管理工具：pnpm
核心框架：Next.js 15
前端框架：React 18
开发语言：TypeScript
样式方案：TailwindCSS v3
Mock 工具：MSW
代码规范：ESLint + Prettier
样式校验：prettier-plugin-tailwindcss
Git 门禁：Husky + lint-staged + commitlint
代码管理：Git + GitHub Desktop

### 项目启动命令为

pnpm dev

### 基于字节跳动平台的主题颜色，本周在tailwind.config.js中配了基础主题色

douyin.red
douyin.cyan
douyin.dark
douyin.panel

## 路由结构与页面模块划分

/ 自动跳转到 /dashboard
/dashboard 平台数据大盘
/creators 创作者分析
/content 内容管理
/audience 观众画像
/assistant 创作助手
/settings 系统设置

对应的App Router目录为 src/app/(workspace)/

| 路由                  | 页面         | 说明                                                     |
| --------------------- | ------------ | -------------------------------------------------------- |
| `dashboard/page.tsx`  | 平台数据大盘 | 展示平台核心指标和趋势                                   |
| `/content/page.tsx`   | 内容管理     | 管理和分析平台视频内容                                   |
| `/audience/page.tsx`  | 观众画像     | 展示用户画像和下钻分析                                   |
| `/assistant/page.tsx` | 创作助手     | 分析热点内容、发布时间和标题词频                         |
| `/settings/page.tsx`  | 系统设置     | 基础设置页，低优先级                                     |
| `/`                   | 首页重定向   | 默认跳转到 `/dashboard`                                  |
| `layout.tsx`          |              | 负责平台后台的整体布局，包括侧边栏、顶部栏和主内容区域。 |

| 业务模块目录结构如下                                                              |
| --------------------------------------------------------------------------------- |
| `src/components/`                                                                 |
| `src/features/`                                                                   |
| `src/services/`                                                                   |
| `src/stores/`                                                                     |
| `src/hooks/`                                                                      |
| `src/lib/`                                                                        |
| `src/types/`                                                                      |
| `src/mocks/`                                                                      |
| 这样的目录划分可以让页面、组件、接口、类型、Mock 数据分别管理，避免后续代码混乱。 |

## ESLint

用于检查代码适量，包括TypeScript、React、Next.js相关规则，以及未使用变量、any使用等问题

相关配置文件： eslint.config.mjs
运行命令： pnpm lint

## Prettier

用于同一代码格式，包括缩进、分号、引号、换行和最大行宽

相关配置文件：
.prettierrc.json
.prettierignore

运行命令：
pnpm format
pnpm format：check

## Tailwind class排序

主要通过 prettier-plugin-tailwindcss 保证className中的样式顺序一致

## 配置Git提交门禁

主要使用：
Husky
lint-staged
commitlint

相关配置包括：
.husky/pre-commit 用于提交前执行lint-staged
.husky/commit-msg 用于校验commit message
commitlint.config.mjs 用于检查已经暂存的文件
lint-staged.config.mjs 用于限制提交信息格式

### 错误提交示例

git commit -m "test" 该提交会被拦截

### 正确示例

git commit -m "chore(config): setup project foundation"

## 接入MSW Mock 服务

主要相关文件包括：
public/mockServiceWorker.js

src/mocks/browser.ts
src/mocks/msw-provider.tsx
src/mocks/handlers.ts
src/mocks/handlers/dashboard.ts
src/mocks/data/dashboard.ts

src/types/api.ts
src/types/dashboard.ts
src/lib/request.ts
src/services/dashboard.ts

### 已经实现的接口

GET /api/dashboard/overview
作为测试接口，使用mock data来返回 创作者总数、活跃创作者、视频总量、平均互动率

## 封装基础请求层

对已有接口进行封装避免在业务组件中直接使用fetch
src/lib/request.ts
并封装了dashboard模块的service： src/services/dashboard.ts

## 实验结果

1. 项目可以通过 pnpm dev 正常启动。
2. /dashboard、/creators、/content、/audience、/assistant、/settings 等路由可以正常访问。
3. 首页 / 可以自动跳转到 /dashboard。
4. ESLint 可以正常执行代码质量检查。
5. Prettier 可以正常执行代码格式化。
6. Tailwind class 排序规范已经生效。
7. Husky、lint-staged 和 commitlint 已经配置完成。
8. 不规范 commit message 会被拦截。
9. MSW 可以正常启动并拦截接口请求。
10. /api/dashboard/overview 可以返回模拟数据。
11. Dashboard 页面可以展示 Mock 接口返回的平台指标数据。

## 遇到过的问题

### Tailwind 样式规范配置报错

在接入 prettier-plugin-tailwindcss 时，曾出现插件找不到 tailwind.config.js 的问题。主要原因是 Prettier 配置路径和项目目录层级不一致。
解决方案: 确认 .prettierrc.json 放在项目根目录、移除错误的 tailwindConfig 路径配置

### Git 仓库根目录配置错误

最初 Git 仓库被初始化在外层目录，导致 Husky 的 .husky 目录没有作为真正的 Git hooks 生效，git commit -m "test" 没有被拦截。

解决方案：
在项目真实根目录重新执行 git init，重新配置 Husky，再次测试 commitlint

### GitHub Desktop 提交被 commitlint 拦截

在 GitHub Desktop 中提交 README 时，默认提交信息为 Update README.md，不符合 commitlint 规范，因此提交失败。

解决方案：docs(readme): update project readme 说明 GitHub Desktop 中也可以正常触发 Husky 和 commitlint。

### MSW 接口请求返回 404或HTML

最开始请求 /api/dashboard/overview 时，接口没有被 MSW 正确拦截，导致请求打到 Next.js 服务器并返回 404 或 HTML 页面。

解决方案： 忘记在根布局接入MswProvider 最终 Mock 接口可以正常返回 JSON 数据。
