# 第七周实验报告：性能深度优化与质量保障

## 一、实验主题

本周实验主题为 **性能深度优化与质量保障**。

在前六周完成数据大盘、内容管理、观众画像和创作助手等核心业务模块后，第七周开始对项目进行系统性的性能优化和质量保障建设。本周重点围绕 React 18 并发渲染优化、组件级懒加载、静态资源优化、单元测试覆盖、Bundle Analyzer 包体积分析和 Lighthouse 性能审计展开。

根据项目大纲，第七周任务包括：应用 React 18 并发特性优化渲染体验；实现路由级与组件级代码分割、懒加载；优化图片、字体和静态资源；编写核心组件与工具函数单元测试；使用 Lighthouse 做性能审计并进行专项优化。交付物包括性能优化专项报告、单元测试用例集、Lighthouse 审计报告和优化后的项目代码。

---

## 二、实验目标

本周实验目标主要包括：

1. 建立前端单元测试环境。
2. 为核心工具函数和核心业务组件补充测试用例。
3. 配置核心组件测试覆盖率阈值。
4. 使用 React 18 并发特性优化大数据列表更新体验。
5. 对 Dashboard、Audience、Assistant 中的重型模块进行动态加载。
6. 使用 Bundle Analyzer 分析客户端包体积。
7. 将 D3 全量导入优化为子包按需导入。
8. 优化图片、字体和静态资源缓存策略。
9. 使用 Lighthouse 对核心页面进行性能审计。
10. 针对 Lighthouse 结果进行专项优化。
11. 输出性能优化记录和实验报告。

---

## 三、实验环境

本周新增依赖：

```bash
pnpm add -D vitest @vitest/coverage-v8 jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
pnpm add -D @next/bundle-analyzer cross-env
pnpm add d3-array d3-geo d3-scale d3-interpolate
pnpm add -D @types/d3-array @types/d3-geo @types/d3-scale @types/d3-interpolate
```

常用检查命令：

```bash
pnpm lint
pnpm type-check
pnpm test
pnpm test:coverage
pnpm build
pnpm build:analyze
```

---

## 四、实验内容与实现过程

## 4.1 接入 Vitest 单元测试框架

本周首先接入了 Vitest 作为项目单元测试框架，并配置了 Testing Library 用于测试 React 组件。

新增配置文件：

```txt
vitest.config.ts
src/test/setup.ts
```

在 `vitest.config.ts` 中配置：

```txt
test environment: jsdom
setupFiles: src/test/setup.ts
coverage provider: v8
coverage reporter: text / html / json-summary
```

同时在 `src/test/setup.ts` 中补充了测试环境所需的 mock，例如：

```txt
matchMedia
ResizeObserver
```

这样可以避免部分组件在 jsdom 环境中因为浏览器 API 不存在而报错。

---

## 4.2 配置核心组件测试覆盖率

项目最初将覆盖率统计范围设置为整个 `src` 目录，导致大量未测试的页面、service、mock、app route 文件被计入覆盖率，整体覆盖率偏低。

本周对覆盖率统计范围进行了调整，将第七周测试覆盖重点限定为核心组件和工具函数，例如：

```txt
src/lib/format.ts
src/lib/utils.ts
src/components/common/empty-state.tsx
creator-assistant 核心展示组件
```

这样更符合第七周“核心组件测试覆盖率”的验收口径，而不是要求全项目所有文件一次性达到 70%。项目大纲中也明确要求核心组件测试覆盖率达到 70%。

---

## 4.3 工具函数测试

本周优先为稳定的工具函数补充单元测试，包括：

```txt
src/lib/format.ts
src/lib/utils.ts
```

测试覆盖内容包括：

```txt
formatInteger
formatDecimal
formatCompactNumber
formatPercent
formatSignedPercent
cn
```

其中 `formatCompactNumber` 用于将大数字格式化为“万 / 亿”等中文单位，是 Dashboard、Content、Audience、Assistant 多个模块的公共依赖。`cn` 用于合并 Tailwind class，是 shadcn/ui 组件和自定义组件中常用的工具函数。

---

## 4.4 核心组件测试

本周为创作助手模块中的核心组件补充了单元测试，包括：

```txt
CreatorAssistantTitleKeywordSection
CreatorAssistantSuggestionSection
CreatorAssistantPublishTimeSection
CreatorAssistantCategoryTrendSection
EmptyState
```

测试内容包括：

1. 组件是否正常渲染标题和核心内容。
2. 默认选中状态是否正确。
3. 用户点击后详情面板是否切换。
4. 空数据状态是否正确展示。
5. 推荐分、趋势状态、建议文案是否展示正确。

在测试过程中，由于部分文案会同时出现在卡片摘要和详情面板中，因此不能使用 `getByText` 断言唯一元素，而是改为使用：

```txt
getAllByText
findByText
findAllByText
```

对于 Framer Motion 驱动的状态切换，使用 `findByText` 或 `waitFor` 等异步断言方式，避免组件状态尚未完成更新时导致测试失败。

---

## 4.5 React 18 并发特性优化

本周在内容管理模块中接入了 React 18 并发特性，重点优化十万级内容列表的筛选、排序和更新体验。

涉及文件：

```txt
src/features/content/components/content-page-content.tsx
```

主要使用：

```txt
useTransition
useDeferredValue
```

优化前，筛选或排序时页面会直接进入 loading 状态，列表可能出现明显闪烁。优化后：

```txt
1. 保留旧列表展示
2. 使用低优先级 transition 更新新列表
3. 顶部显示“列表更新中...”提示
4. 使用 deferredItems 渲染虚拟列表
```

这样可以让用户操作优先响应，降低大列表状态更新对交互体验的影响。

---

## 4.6 Dashboard 图表模块懒加载

Dashboard 页面中多个图表模块依赖 ECharts。ECharts 是当前项目中体积最大的业务依赖之一，因此本周对 Dashboard 中的图表 section 进行了动态加载。

懒加载模块包括：

```txt
DashboardTrendSection
DashboardCategorySection
DashboardPublishTrendSection
```

优化后，Dashboard 页面会优先展示：

```txt
时间筛选
核心指标卡片
```

然后异步加载趋势图、分类占比图和发布趋势图。这样可以降低 Dashboard 首屏主路径压力。

---

## 4.7 Audience D3 可视化模块懒加载

Audience 页面中包含两个较重的 D3 自定义可视化模块：

```txt
ChinaAudienceHeatmap
AudienceKeywordSection
```

本周将区域热力图和关键词云模块通过 `next/dynamic` 进行组件级懒加载，并设置 `ssr: false`，避免 D3 和地图 GeoJSON 相关逻辑进入首屏同步渲染路径。

优化后，Audience 页面会优先展示：

```txt
中国区画像概览
性别 / 年龄 / 终端分布基础图表
```

然后异步加载 D3 中国地图热力图和关键词云。

---

## 4.8 Assistant Framer Motion 模块懒加载

创作助手模块使用了 Framer Motion 实现交互动效，包括建议卡片 hover、点击缩放、详情切换动画和页面分区进入动画。

由于 Framer Motion 体积较大，本周将以下模块改为动态加载：

```txt
CreatorAssistantTitleKeywordSection
CreatorAssistantSuggestionSection
```

这样 Framer Motion 相关代码主要在 Assistant 交互模块中加载，避免影响其他页面首屏。

---

## 4.9 D3 按需导入优化

Bundle Analyzer 初始结果显示，D3 曾以完整包形式进入客户端 bundle，表现为：

```txt
d3 index.js + 168 modules
```

本周将：

```ts
import * as d3 from 'd3';
```

优化为 D3 子包按需导入，例如：

```ts
import { max } from 'd3-array';
import { geoMercator, geoPath } from 'd3-geo';
import { interpolateRgb } from 'd3-interpolate';
import { scaleSequential, scaleSqrt } from 'd3-scale';
```

关键词云模块中也改为按需导入：

```ts
import { extent } from 'd3-array';
import { scaleOrdinal, scaleSqrt } from 'd3-scale';
```

优化后，Bundle Analyzer 中不再出现完整的 `d3 index.js + 168 modules`，而是只保留地图和比例尺相关子模块。这说明按需导入优化已经生效。

---

## 4.10 图片、字体与静态资源优化

本周对 `next.config.ts` 进行了静态资源优化配置，包括：

```txt
1. 配置 next/image 支持 avif 和 webp
2. 配置远程图片域名 i.ytimg.com 和 img.youtube.com
3. 为地图 GeoJSON 配置长期缓存
4. 为 public/mock 静态数据配置缓存
```

同时，字体策略采用系统字体，不额外引入外部 Google Font，避免字体资源请求影响 Lighthouse 分数。

全局字体配置思路为：

```txt
Inter
ui-sans-serif
system-ui
-apple-system
BlinkMacSystemFont
Segoe UI
sans-serif
```

这样可以降低外部字体加载成本，并保证跨平台显示稳定。

---

## 4.11 Bundle Analyzer 分析

本周接入了：

```txt
@next/bundle-analyzer
```

并新增分析命令：

```bash
pnpm build:analyze
```

由于当前 Next.js 构建默认使用 Turbopack，而 `@next/bundle-analyzer` 不兼容 Turbopack 构建，因此分析命令中单独使用 Webpack：

```bash
next build --webpack
```

Bundle Analyzer 结果显示，当前客户端主要体积来源包括：

```txt
1. ECharts
2. React / Next.js runtime
3. MSW
4. Framer Motion
5. D3 子模块
```

其中：

- ECharts 是图表页面的主要依赖。
- MSW 进入生产 bundle 是因为当前项目本地生产模式仍然依赖 MSW Mock。
- Framer Motion 主要用于创作助手动效。
- D3 已由全量包优化为子包按需导入。
- 未发现 `content-list.json` 直接进入客户端 JS bundle，说明十万级内容数据已从业务 bundle 中剥离。

---

## 4.12 生产模式下 MSW 的处理

本项目当前仍然是前端实习项目，生产模式主要用于本地演示和 Lighthouse 性能审计。因此，项目允许在生产模式下继续启用 MSW，用于拦截 `/api/*` 请求。

该方案的优点是：

```txt
1. 本地生产环境可以继续使用 Mock API
2. 无需立即搭建真实后端
3. 方便演示完整业务流程
```

但该方案也有成本：

```txt
1. MSW 浏览器端代码会进入 client bundle
2. Lighthouse 会统计这部分 JS 体积
3. 真实线上环境不建议长期依赖浏览器端 MSW
```

因此，文档中明确说明：当前生产模式下使用 MSW 是为了本地演示和性能测试，未来真实上线时可替换为真实后端 API 或 Next.js Route Handlers。

---

## 4.13 Lighthouse 基线与最终结果

本周使用 Lighthouse 对四个核心页面进行性能审计：

```txt
/dashboard
/content
/audience
/assistant
```

最终结果如下：

| 页面         | Performance | Accessibility | Best Practices | SEO |  FCP |  LCP |   TBT |   CLS | 结论     |
| ------------ | ----------: | ------------: | -------------: | --: | ---: | ---: | ----: | ----: | -------- |
| `/dashboard` |         100 |            95 |            100 | 100 | 0.2s | 0.8s |   0ms | 0.003 | 达标     |
| `/content`   |          85 |            92 |            100 | 100 | 0.2s | 0.7s | 340ms |     0 | TBT 偏高 |
| `/audience`  |          99 |            95 |            100 | 100 | 0.2s | 0.8s |   0ms |     0 | 达标     |
| `/assistant` |         100 |            95 |            100 | 100 | 0.2s | 0.7s |   0ms |     0 | 达标     |

从结果看，Dashboard、Audience、Assistant 三个页面均达到 Performance 90+，并且 FCP / LCP 均低于 1.5s。Content 页面 Accessibility 已优化到 92，但 Performance 为 85，主要扣分项为 TBT。

---

## 4.14 Content 页面 TBT 说明

Content 页面在 Lighthouse 审计中 Performance 为 85，主要原因是 Total Blocking Time 约为 340ms。

该问题的主要原因并不是虚拟列表渲染失效，而是 Content 页面为了验证十万级内容管理场景，保留了一次性加载 100000 条 Mock 数据的压力测试口径。

当前页面使用 `react-window` 实现虚拟滚动，实际 DOM 渲染行数只保持在可视区域附近的几十行，因此 Rendering 和 Painting 压力较低。但在数据层面，浏览器仍需要完成：

```txt
1. 大 JSON 加载
2. JSON parse
3. Mock 层筛选
4. Mock 层排序
5. 状态更新
6. 虚拟列表数据传入
```

这些工作会占用主线程，从而造成 TBT 偏高。

因此，Content 页面当前性能瓶颈主要集中在大数据处理阶段，而不是列表 DOM 渲染阶段。后续真实业务场景中可以通过服务端分页、接口级排序、增量加载、Web Worker 数据处理或预排序数据等方式进一步降低主线程阻塞。

---

## 五、实验结果

本周完成了第七周性能深度优化与质量保障建设，主要结果如下：

1. 成功接入 Vitest 和 Testing Library。
2. 为核心工具函数和核心组件补充了单元测试。
3. 配置了核心组件覆盖率阈值。
4. 使用 React 18 并发特性优化了 Content 大列表更新体验。
5. 对 Dashboard ECharts 图表模块进行了动态加载。
6. 对 Audience D3 地图和关键词云进行了动态加载。
7. 对 Assistant Framer Motion 交互模块进行了动态加载。
8. 将 D3 全量导入优化为子包按需导入。
9. 使用 Bundle Analyzer 分析客户端包体积。
10. 完成图片、字体和静态资源缓存优化。
11. 完成 Lighthouse 四个核心页面审计。
12. Dashboard、Audience、Assistant 页面性能指标均达标。
13. Content 页面可访问性已达标，TBT 偏高原因已定位并记录。
14. 未发现严重内存泄漏或明显渲染瓶颈。

---

## 六、遇到的问题与解决方案

### 6.1 Bundle Analyzer 不支持 Turbopack 构建

问题表现：

运行分析命令时提示：

```txt
The Next Bundle Analyzer is not compatible with Turbopack builds
```

原因分析：

当前项目普通 build 使用 Turbopack，而 `@next/bundle-analyzer` 需要 Webpack 构建模式才能生成分析报告。

解决方案：

新增单独分析命令：

```txt
build:analyze
```

并在命令中使用：

```txt
next build --webpack
```

这样普通构建仍然可以保留 Turbopack，包体积分析时单独使用 Webpack。

---

### 6.2 D3 全量导入导致 bundle 体积偏大

问题表现：

Bundle Analyzer 中出现：

```txt
d3 index.js + 168 modules
```

原因分析：

组件中使用了：

```ts
import * as d3 from 'd3';
```

导致完整 D3 包进入客户端 bundle。

解决方案：

改为从 D3 子包按需导入：

```txt
d3-array
d3-geo
d3-scale
d3-interpolate
```

优化后 Bundle Analyzer 中只保留地图投影、比例尺、颜色插值等实际用到的模块。

---

### 6.3 生产模式下 MSW 接口失效

问题表现：

在 `pnpm build && pnpm start` 后，页面 `/api/*` 请求返回 404。

原因分析：

MSW 浏览器模式依赖 Service Worker 拦截请求。如果生产环境中 MSW 没有启动，或者页面在 worker 启动前已经发起请求，请求会直接打到 Next.js 服务端，导致 404。

解决方案：

保留生产模式下使用 MSW 的策略，并确保：

```txt
1. NEXT_PUBLIC_API_MOCKING=enabled 在 build 前存在
2. public/mockServiceWorker.js 存在
3. MswProvider 包裹全局页面
4. 页面等待 worker.start() 完成后再渲染业务组件
```

同时在报告中明确说明：生产模式依赖 MSW 主要用于本地演示和前端 Mock，真实上线环境可替换为真实 API 或 Next.js Route Handlers。

---

### 6.4 测试中出现 MultipleElementsFoundError

问题表现：

Testing Library 中使用：

```txt
getByText
```

断言某些文案时失败，提示找到多个匹配元素。

原因分析：

组件中同一句文案可能同时出现在卡片摘要和详情面板中，例如创作建议标题、标题关键词建议文案等。

解决方案：

将断言方式改为：

```txt
getAllByText
findByText
findAllByText
waitFor
```

其中：

```txt
初始唯一文本 -> getByText
可能重复文本 -> getAllByText
点击后异步出现文本 -> findByText / findAllByText
```

---

### 6.5 Content 页面 TBT 偏高

问题表现：

Content 页面 Lighthouse Performance 为 85，TBT 约为 340ms。

原因分析：

Content 页面为了保留十万级内容管理压力测试场景，一次性加载和处理 100000 条 Mock 数据。虚拟列表减少了 DOM 渲染压力，但不能减少数据加载、解析、筛选和排序成本。

解决方案：

本周已完成：

```txt
1. 使用 react-window 控制实际 DOM 行数
2. 使用 React 18 useTransition / useDeferredValue 优化列表更新体验
3. 修复 Accessibility 问题
4. 对默认排序进行优化
5. 在性能报告中记录 TBT 偏高原因和后续优化方向
```

后续真实业务场景可通过服务端分页、接口排序、Web Worker 或增量加载进一步优化。

---

## 七、第七周交付物

本周交付物如下：

```txt
1. Vitest 测试配置
   vitest.config.ts

2. 测试环境配置
   src/test/setup.ts

3. 工具函数测试
   src/lib/format.test.ts
   src/lib/utils.test.ts

4. 通用组件测试
   src/components/common/empty-state.test.tsx

5. 创作助手组件测试
   creator-assistant-title-keyword-section.test.tsx
   creator-assistant-suggestion-section.test.tsx
   creator-assistant-publish-time-section.test.tsx
   creator-assistant-category-trend-section.test.tsx

6. React 18 并发优化代码
   src/features/content/components/content-page-content.tsx

7. Dashboard 图表懒加载优化
   src/features/dashboard/components/dashboard-page-content.tsx

8. Audience D3 可视化懒加载优化
   src/features/audience/components/audience-page-content.tsx

9. Assistant 动效模块懒加载优化
   src/features/creator-assistant/components/creator-assistant-page-content.tsx

10. D3 按需导入优化
    china-audience-heatmap.tsx
    audience-keyword-cloud.tsx

11. 静态资源优化配置
    next.config.ts

12. Bundle Analyzer 配置
    next.config.ts
    package.json build:analyze script

13. Lighthouse 审计结果记录
    docs/week-07-performance-notes.md

14. 第七周实验报告
    docs/week-07-lab-report.md
```

---

## 八、第七周验收情况

| 验收项                         | 完成情况             |
| ------------------------------ | -------------------- |
| React 18 并发特性优化          | 已完成               |
| 路由级代码分割                 | App Router 默认支持  |
| 组件级代码分割 / 懒加载        | 已完成               |
| Dashboard ECharts 懒加载       | 已完成               |
| Audience D3 懒加载             | 已完成               |
| Assistant Framer Motion 懒加载 | 已完成               |
| D3 按需导入                    | 已完成               |
| 图片资源优化                   | 已完成               |
| 字体策略优化                   | 已完成               |
| 静态资源缓存策略               | 已完成               |
| 核心组件单元测试               | 已完成               |
| 工具函数单元测试               | 已完成               |
| 核心组件覆盖率阈值配置         | 已完成               |
| Bundle Analyzer 分析           | 已完成               |
| Lighthouse 审计                | 已完成               |
| Dashboard Lighthouse 达标      | 已完成               |
| Audience Lighthouse 达标       | 已完成               |
| Assistant Lighthouse 达标      | 已完成               |
| Content Accessibility 达标     | 已完成               |
| Content Performance            | TBT 偏高，已记录原因 |
| 无严重内存泄漏                 | 未发现明显问题       |
| 无严重渲染瓶颈                 | 基本达成             |

---

## 九、本周总结

第七周主要完成了项目性能优化和质量保障体系建设。本周从测试、构建、资源、渲染和审计多个层面对项目进行了专项优化。

在质量保障方面，项目接入了 Vitest 和 Testing Library，并为核心工具函数、通用组件和创作助手核心组件补充了测试用例，同时配置了核心组件覆盖率阈值。

在性能优化方面，本周使用 React 18 并发特性优化了 Content 大列表更新体验；使用 dynamic import 对 Dashboard、Audience、Assistant 中的重型模块进行懒加载；将 D3 全量导入优化为子包按需导入；通过 Bundle Analyzer 分析出 ECharts、MSW、Framer Motion 和 D3 等主要体积来源；同时补充了图片、字体和静态资源缓存策略。

在 Lighthouse 审计方面，Dashboard、Audience、Assistant 页面均取得了较好的性能结果，Content 页面虽然由于十万级 Mock 数据压力测试导致 TBT 偏高，但其 FCP、LCP、CLS 和可访问性表现已经较好，且瓶颈原因明确、后续优化方向清晰。
